<h1>All Users</h1>

<ul id="users">
  <% @users.each do |user| %>
    <li>
    <%= link_to user.username, user_path(user),
    class: 'user_name', id: "user_#{user.id}" %>
    </li>
  <% end %>
</ul>
<%= link_to "Create a New user", new_user_path, class: "new_user" %>